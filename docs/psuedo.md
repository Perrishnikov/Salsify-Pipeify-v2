```

let totalLength = 30 in #36 # NUTRIENTS let nutrientHeader = RPAD('____NUTRIENTS_','_', totalLength) in #if this is CA, dont show "Nutrients" -- using the IF statement below, returns all items, not just ones with PLM1 data making testing impossible. #let nutrientHeader = IF(EQUAL(VALUE("MARKETING_CODE"),"CANADA"),"",nutrientHeader) in # only show header if there are any values let nutrientHeader = IF(LT(VALUES("PLM1_LDS_NUTRIENT"),1),'',nutrientHeader) in let nutrients = REDUCE(VALUES("PLM1_LDS_NUTRIENT"), (acc,entry) => # row returns array of items let row = SPLIT(entry, "~") in let items = EACH(row, (item,index) => let split = SPLIT(item, "|") in #let order = SQUISH(AT(split,1)) in let shortDesc = SQUISH(REPLACE(AT(split,2),"<.*?>", " ")) in let longDesc = SQUISH(AT(split,3)) in let desc = COALESCE(longDesc, shortDesc) in #coalesce descriptions into one value #"Indent" Added Sugars (done below) #let desc = IF(CONTAINS(desc,"Added Sugars"), # CONCAT("( ", desc, " )"), #desc) in #"Indent" Folic Acid (done below) #let desc = IF(CONTAINS(desc,"Folic"), #CONCAT("( ",desc, " )"), #desc) in let amount = SQUISH(AT(split, 4)) in let uom = LOWER(SQUISH(AT(split,5))) in # Do not show "cal" let uom = SUBSTITUTE(uom,"cal", "") in let dv = SQUISH(AT(split,6)) in let percent = SQUISH(AT(split,7)) in let symbol = SQUISH(AT(split,8)) in #dagger † let def = SQUISH(AT(split,9)) in #9th position might need to be symbol definition... let return = CONCATENATE(desc, ' ', amount, uom, ' ', dv, percent, ' ', symbol) in #"Indent" Added Sugars let return = IF(CONTAINS(desc,"Added Sugars"), CONCAT("( ", return, " )"), return) in #"Indent" Folic Acid let return = IF(CONTAINS(desc,"Folic"), CONCAT("( ",return, " )"), return) in #"Indent" Quatrefoli let return = IF(CONTAINS(desc,"Quatrefoli"), CONCAT("( ",return, " )"), return) in #process each item in ALL entries and add it to the array CONCAT_ARRAYS(acc,return) ) in items ,nutrientHeader) in # collect all nutrient definitions in array let nutrientsDef = REDUCE(VALUES("PLM1_LDS_NUTRIENT"), (acc,entry) => # row returns array of items let row = SPLIT(entry, "~") in let items = EACH(row, (item,index) => let split = SPLIT(item, "|") in let def = SQUISH(AT(split,9)) in #9th position might need to be symbol definition... let return = SQUISH(def) in #process each item in ALL entries and add it to the array CONCAT_ARRAYS(acc,return) ) in items ,'') in # INGREDIENTS let df = VALUE("DOSAGE_FORM") in let df = SUBSTITUTE(df,"Enteric-Coated Softgel Capsule", "Softgel") in let df = SUBSTITUTE(df,"Ent Coat Sftgl", "Softgel") in # CA sofgels get "per serving", not "per pill" like other CA. let df = IF( AND( EQUAL(df,"Softgel"), EQUAL(VALUE("MARKETING_CODE"),"CANADA")), "Serving", df) in let ingredHeader = IF( EQUAL(VALUE("MARKETING_CODE"),"CANADA") ,CONCAT("____Each ",df , " Contains Medicinal Ingredients:") ,RPAD("____INGREDIENTS","_",totalLength) ) in #if this is CA, replace "ingredients" with Medicinal # only show header if there are any values let ingredHeader = IF(LT(VALUES("PLM1_RAW_MAT_QTY_DRAFT"),1),'',ingredHeader) in let ingreds = REDUCE(VALUES("PLM1_RAW_MAT_QTY_DRAFT"), (acc,entry) => # row returns array of items let row = SPLIT(entry, "~") in let items = EACH(row, (item, index) => let split = SPLIT(item, "|") in #let order = SQUISH(AT(split,1)) in let desc = SQUISH(AT(split,2)) in #"Indent" Sub Omegas let desc = IF(OR( CONTAINS(desc,"Eicosapentaenoic"), AND(CONTAINS(desc,"Docosahexaenoic"),NOT(CONTAINS(desc,"Algal"))), CONTAINS(desc,"Other Omega-3"), CONTAINS(desc,"Caprylic Acid"), AND(CONTAINS(desc,"Capric Acid"),NOT(CONTAINS(desc,"Medium Chain Triglycerides"))) ) ,CONCAT("( ",desc, " )") ,desc) in #"Indent" bacteria, but only if "blend" is in the desc let desc = IF(AND( CONTAINS(entry,'Blend') ,OR( CONTAINS(desc,"Bifidobacterium") ,CONTAINS(desc,"Lactobacillus") ) ) ,CONCAT("( ",desc, " )") ,desc) in let amount = SQUISH(AT(split, 3)) in let uom = SQUISH(AT(split,4)) in let uom = IF( OR( CONTAINS(uom,"DP"), CONTAINS(uom,"DU"), CONTAINS(uom,"USP"), CONTAINS(uom,"CU"), CONTAINS(uom,"FIP"), CONTAINS(uom,"HUT"), CONTAINS(uom,"PC"), CONTAINS(uom,"ALU"), CONTAINS(uom,"SU"), CONTAINS(uom,"FTU") ), uom, uom ) in let symbol = SQUISH(AT(split,5)) in #** let def = SQUISH(AT(split,6)) in #**Daily Value no let return = CONCATENATE(desc, ' ', amount, uom, ' ', symbol) in #process each item in ALL entries and add it to the array CONCAT_ARRAYS(acc,return) ) in items ,ingredHeader) in # collect all ingredient definitions in array let ingredsDef = REDUCE(VALUES("PLM1_RAW_MAT_QTY_DRAFT"), (acc,entry) => # row returns array of items let row = SPLIT(entry, "~") in let items = EACH(row, (item,index) => let split = SPLIT(item, "|") in let def = SQUISH(AT(split,6)) in let return = SQUISH(def) in #process each item in ALL entries and add it to the array CONCAT_ARRAYS(acc,return) ) in items ,'') in # OTHER INGREDIENTS let otherIngredsHeader = IF( EQUAL(VALUE("MARKETING_CODE"),"CANADA") ,"____Non-medicinal Ingredients:" ,RPAD("____OTHER INGREDIENTS","_",totalLength) ) in # only show header if there is a value let otherIngredsHeader = IF(LT(VALUES("PLM1_INGREDIENT_DRAFT_TEXT"),1),'',otherIngredsHeader) in #create entity for Other Ingredients let otherIngreds = reduce(VALUES("PLM1_INGREDIENT_DRAFT_TEXT"), (acc,entry) => CONCAT_ARRAYS(acc,entry) ,otherIngredsHeader) in # DEFINITIONS let definitionsHeader = RPAD('_','_', totalLength) in # only show header if there is a value #let definitionsHeader = IF(LT(AND(VALUES(nutrientsDef),VALUES(ingredsDef)),1),'',definitionsHeader) in let definitionsHeader = IF(OR(GT(LENGTH(VALUES(nutrientsDef)),0),GT(LENGTH(VALUES(ingredsDef)),0)),definitionsHeader,'') in #start with line, include unique definitions from above let definitions = REDUCE(UNIQ(CONCAT_ARRAYS(nutrientsDef, ingredsDef)), (acc,entry) => CONCAT_ARRAYS(acc,entry) ,definitionsHeader ) in # FINAL let final = COMPACT(CONCAT_ARRAYS(nutrients,ingreds,otherIngreds,definitions)) in IF(EQUAL(VALUE("Product Type"),"EA"), final ) 
```